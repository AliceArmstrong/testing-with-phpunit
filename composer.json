{
    "scripts": {
        "test": "./vendor/bin/phpunit --testdox --colors=always",
        "test-runtime": "./vendor/bin/phpunit --testdox --verbose --colors=always",
        "test-original": "./vendor/bin/phpunit"
    },
    "require-dev": {
        "phpunit/phpunit": "^9"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    }
}
